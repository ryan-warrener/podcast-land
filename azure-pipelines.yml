  trigger:
  - master

  pool:
    vmImage: 'Ubuntu-latest'
  
  variables:
    buildConfiguration: release

  steps:
  - task: DotNetCoreCLI@2
    enabled: true
    displayName: 'Build'
    inputs:
      command: 'build'

  - task: DotNetCoreCLI@2
    enabled: true
    displayName: 'Publish'
    inputs:
      command: 'publish'
      publishWebProjects: true
      arguments: '--configuration $(buildConfiguration)  --output $(Build.ArtifactStagingDirectory)/codedeploy/app'
      zipAfterPublish: false

  - task: PublishBuildArtifacts@1
    enabled: false
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/codedeploy/app'
      ArtifactName: 'app'
      publishLocation: 'Container'

  - task: CopyFiles@2
    enabled: true
    inputs:
      SourceFolder: 'codedeploy/'
      Contents: '**'
      TargetFolder: '$(Build.ArtifactStagingDirectory)/codedeploy/'

  - task: PublishBuildArtifacts@1
    enabled: false
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/codedeploy/'
      ArtifactName: 'codedeploy'
      publishLocation: Container

  - task: ArchiveFiles@2
    enabled: true
    inputs:
      rootFolderOrFile: '$(Build.ArtifactStagingDirectory)/codedeploy/'
      includeRootFolder: false
      archiveType: 'tar'
      tarCompression: none 
      archiveFile: '$(Build.ArtifactStagingDirectory)/codedeploy.tar'
      replaceExistingArchive: true

  - task: PublishBuildArtifacts@1
    enabled: false
    inputs:
      PathtoPublish: '$(Build.ArtifactStagingDirectory)/codedeploy.tar'
      ArtifactName: 'tar'
      publishLocation: Container

  - task: CodeDeployDeployApplication@1
    enabled: true
    inputs:
      awsCredentials: 'AWS CodeDeploy Bucket'
      regionName: 'us-east-1'
      applicationName: 'podcast-land'
      deploymentGroupName: 'linux-dg'
      deploymentRevisionSource: 'workspace'
      revisionBundle: '$(Build.ArtifactStagingDirectory)/codedeploy.tar'
      bucketName: 'ryan-warrener-codedeploy'